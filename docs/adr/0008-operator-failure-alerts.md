# ADR-0008: T-116 운영자 장애 알림 채널

- 상태: Implemented — 2026-09-11 현재 코드에 맞춰 기록 정정 (새 채널 결정 없음)
- 범위: T-116의 재시도 소진·스케줄러 오류를 운영자에게 전달하는 경로

## 구현 전 배경

배송은 최대 3회 시도하며 404/410 구독을 비활성화한다. 재시도 소진과 스케줄러
예외는 로그로만 남는다. 운영자가 로그를 보지 않으면 장애를 알 수 없다.

## 초기 제안 (아래 구현 현황으로 구체화)

운영자가 지정한 HTTPS 웹훅을 별도 장애 알림 채널로 둔다. 일반 구독자에게 장애
알림을 보내지 않는다. 수신 URL은 환경 변수로만 설정하고 코드·로그에는 노출하지 않는다.
재시도 소진은 DB의 실패 기록을 기반으로 감지하고 중복 발송을 제한한다.
알림 전송 실패가 정상 배송 트랜잭션을 롤백시키지 않게 분리한다.
스케줄러/DB 오류는 DB 기록에 의존하지 않는 경로로도 알린다.

기본안은 Discord/Slack 등 운영자가 관리하는 웹훅이다. 구독자용 Web Push의 키나
발송 서비스 장애와 독립적으로 알릴 수 있다. 구체적인 수신 채널과 활성화 설정은
운영자의 선택이 필요하다. 실제 시험 메시지는 별도 승인 없이 전송하지 않는다.

## 대안

- 운영자용 Web Push 구독을 별도로 지정: 별도 서비스 없이 이용 가능하지만
  일반 알림과 전송 인프라가 같아 VAPID/푸시 서비스 장애 때 함께 실패할 수 있다.
- 관리자 화면 경고만 표시: 외부 설정은 없지만 화면을 열지 않으면 알 수 없어
  현재 T-116의 외부 장애 알림 완료 조건을 충족하지 않는다.

## 검증

실제 네트워크 대신 가짜 발송기와 HTTP transport로 의도적 실패, 재시도 제한,
404/410 비활성화, 장애 알림 중복 제한, 장애 알림 실패 시 정상 배송 보존을 검증한다.

## 과거 기록: 채널 결정 전 검증 결과 — 2026-09-11

- make lint 통과, make test: 398 passed / 기존 2 xfailed.
- 격리 DB 9개 시나리오 통과. 최대 3회 재시도 소진 및 한 발송기의 예외 후 다른 배송 계속 처리 포함.
- 가짜 HTTP 발송으로 404/410은 GONE, 429/500/503은 FAILED 분류 확인.
- 기존 데이터 변경이나 실제 시험 알림은 없음. 운영자 외부 알림은 아직 구현하지 않았으며 수신 채널 선택 대기 중.

## 현재 구현 — 2026-09-11

현재 소스의 `SlackAlerter`와 스케줄러 연결을 기준으로 기록한다. 초기 제안의
DB 기반 억제는 채택되지 않았으며, 이 문서 정정으로 설정이나 전송 동작을 바꾸지 않는다.

- 채널: Slack Incoming Webhook. collector의 `SLACK_WEBHOOK_URL`이 공백이면 비활성화된다.
  Compose가 collector에 전달하며, 환경 변경은 컨테이너 재생성이 필요하다.
- 대상: 스케줄러 주기 예외(`scheduler.tick_failed`)와 해당 주기에 새로 재시도가 소진된 배송
  (`push.retry_exhausted`). 일반 재시도 실패와 404/410 구독 비활성화는 별도 경보를 보내지 않는다.
- 트랜잭션: 정상 배송 커밋 이후 소진을 보고한다. DB/주기 오류도 별도 경로로 전송한다.
  Slack 전송 실패나 예외는 정상 배송을 롤백시키지 않는다. HTTP 제한 시간은 5초다.
- 억제: `ThrottledAlerts`가 성공 전송한 key를 프로세스 메모리에 기록한다.
  기본 `ALERT_COOLDOWN=None`은 같은 key를 프로세스당 한 번만 보낸다는 뜻이다.
  문제가 해결된 뒤 재발해도 다시 알리지 않으며 재시작하면 기록이 초기화된다.
  Slack 전송 실패는 성공으로 기록하지 않는다. 다만 소진 건은 새 소진이 있는 주기에만 보고하므로,
  Slack 실패 후 해당 경보를 내구성 있게 저장해 재전송하는 큐는 없다.
- 한계: 프로세스/호스트가 완전히 중단되면 이 경로로는 알릴 수 없다. 외부 생존 감시,
  복구 알림, DB에 보존되는 경보 큐, 파서 장애 알림은 미구현이다.
- 보안: 설정의 DB URL·관리자 키·VAPID 개인키·웹훅 URL은 repr에서 제외한다.
  HTTP 전송 예외는 클래스명만 기록하여 URL이 섞인 예외 문자열을 남기지 않는다.

근거: [스케줄러](../../apps/collector/src/vinyl_collector/scheduler.py),
[Slack 발송기](../../apps/collector/src/vinyl_collector/slack_alerter.py),
[경보 억제](../../packages/core/src/vinyl_core/alerts.py),
[설정](../../packages/core/src/vinyl_core/settings.py).

T-012 작업에서는 운영 웹훅 설정이나 실제 Slack 수신을 검증하지 않았다.
초기 기록의 테스트 수치는 당시 결과이며 최신 검증 결과는
[문서 정합성 점검의 후속 기록](../documentation-review.md)을 참고한다.
